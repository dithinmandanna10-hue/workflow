pandas>=2.0
numpy>=1.24
python-dotenv>=1.0
matplotlib>=3.7
# MetaTrader5 only works on Windows with MT5 installed
# pip install MetaTrader5  # uncomment on Windows
# For Linux/Mac you can still run backtest & dry-run without it
